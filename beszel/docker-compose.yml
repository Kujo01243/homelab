services:
  beszel:
    image: henrygd/beszel
    container_name: beszel
    restart: unless-stopped
    volumes:
      - /root/beszel:/beszel_data
    networks:
      - beszel
    environment:
      USER_CREATION: true

networks:
  beszel:
    external: true